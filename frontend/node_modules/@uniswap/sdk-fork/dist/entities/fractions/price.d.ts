import { BigintIsh, Rounding } from '@uniswap/sdk-fork/dist/constants';
import { Token } from '@uniswap/sdk-fork/dist/entities/token';
import { Route } from '@uniswap/sdk-fork/dist/entities/route';
import { Fraction } from '@uniswap/sdk-fork/dist/entities/fractions/fraction';
import { TokenAmount } from '@uniswap/sdk-fork/dist/entities/fractions/tokenAmount';
export declare class Price extends Fraction {
    readonly baseToken: Token;
    readonly quoteToken: Token;
    readonly scalar: Fraction;
    static fromRoute(route: Route): Price;
    constructor(baseToken: Token, quoteToken: Token, denominator: BigintIsh, numerator: BigintIsh);
    get raw(): Fraction;
    get adjusted(): Fraction;
    invert(): Price;
    multiply(other: Price): Price;
    quote(tokenAmount: TokenAmount): TokenAmount;
    toSignificant(significantDigits?: number, format?: object, rounding?: Rounding): string;
    toFixed(decimalPlaces?: number, format?: object, rounding?: Rounding): string;
}
